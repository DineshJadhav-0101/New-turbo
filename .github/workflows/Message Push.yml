name: Message Push

on:
  workflow_dispatch:
    inputs:
      REF_Number:
        description: 'Enter the REF Number'
        required: true
        default: '{"refNo":" "}'  # Corrected JSON format

jobs:
  Tran-Push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Display REF Number
        run: echo "The entered REF Number is ${{ github.event.inputs.REF_Number }}"

      - name: Azure Login
        run: az login --service-principal --username ${{ secrets.AZURE_CLIENT_ID }} --password ${{ secrets.AZURE_CLIENT_SECRET }} --tenant ${{ secrets.AZURE_TENANT_ID }}

      - name: Set Azure subscription
        run: az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Enable Public Network Access (All Networks)
        run: |
          az servicebus namespace update \
            --name Dinesh-Service-Busname \
            --resource-group D-service-bus \
            --public-network-access Enabled

      - name: Send message to queue using REST API
        run: |
          SAS_TOKEN=/subscriptions/da88dd16-869c-47b5-8dec-43b6f64f5004/resourceGroups/D-service-bus/providers/Microsoft.ServiceBus/namespaces/Dinesh-Service-Busname/authorizationrules/private-end-point
          az rest --method post \
            --url "https://Dinesh-Service-Busname.servicebus.windows.net/feed-file/messages?timeout=60" \
            --headers "Content-Type=application/json" \
            --body '{"refNo":"e3870492-b792-4e81-99fa-653e39905c9d"}' \
            --headers "Authorization=SharedAccessSignature $SAS_TOKEN"

      - name: Disable Public Network Access
        run: |
          az servicebus namespace update \
            --name Dinesh-Service-Busname \
            --resource-group D-service-bus \
            --public-network-access Disabled
